from "../stdlib/SharedPointer.shtk" use SharedPointer, allocateHeap

fn main() -> int
{
    var b := allocateHeap[int](10)
    b.setItem(4, 5)

    println(b.length())
    println(b[4])

    return 0
}

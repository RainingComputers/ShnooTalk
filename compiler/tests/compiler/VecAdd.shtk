use "TestModules/Math.shtk" as math

from math use vec
from vec use Vec

fn main() -> int
{
    var a:Vec, b:Vec, c:Vec, d:Vec

    a.setVec(1, 2)
    b.setVec(5, 10)

    c = vec::__add__(a, b)
    d = c.__add__(a)

    a.printVec()
    b.printVec()
    c.printVec()
    d.printVec()

    return 0
}

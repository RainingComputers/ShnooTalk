from "TestModules/LinkedList.shtk" use LinkedList

fn main() -> int
{
    var list: LinkedList[int] 
    list.init()

    list.push(10)
    list.push(5)
    list.push(23)
    
    list.printList()

    println(list.pop())
    println(list.pop())

    list.printList()
    
    return 0
}

# MODULE assignerr3_test.uhll
# ERROR in Line 15 Col 7
# 
#     a += b
#       ^
# Only EQUAL operator allowed on STRUCT or ARRAY

use vec
from vec use Vec

fn main()
{
    var a:Vec, b:Vec

    a += b
}
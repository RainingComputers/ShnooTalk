# MODULE NonEqualOperatorOnStruct_test.uhll
# ERROR in Line 15 Col 7
# 
#     a += b
#       ^
# Only EQUAL operator allowed on STRUCT and ARRAY

use "vec.uhll" as vec
from vec use Vec

fn main()
{
    var a:Vec, b:Vec

    a += b
}
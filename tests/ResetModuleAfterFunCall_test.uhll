# Pin 0 low
# This works

use "TestModules/Vector.uhll" as vec
use "TestModules/Digital.uhll" as digital

fn main() -> int
{

    var a: vec::Vec
    a.x = 1
    a.y = 1

    digital::digitalWrite(0, 0)

    if(vec::isUnitVector(a) == true)
        println("This works")

    return 0
}
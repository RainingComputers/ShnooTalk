# 3 4 5
# 6 8 10

fn main() -> int
{
    for(var i:int = 1; i <= 10; i += 1)
        for(var j:int = i; j <= 10; j += 1)
            for(var k:int = 1; k <= 10; k += 1)
                if(k * k == i * i + j * j)
                    println(i, j, k)

    return 0
}

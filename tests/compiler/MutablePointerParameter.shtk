fn testfunction(mut p: int*, a: int*)
{
    p <- a
}

fn main() -> int
{
    var x: int = 2
    var y: int = 3

    var p: int* <- x
    var q: int* <- y

    println(p)

    testfunction(p, q)

    println(p)

    return 0
}